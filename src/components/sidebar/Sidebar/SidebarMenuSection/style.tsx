import styled from 'styled-components';

const StyledMenuSection = styled.div`
  display: flex;
  flex-direction: column;

  :first-child {
    margin-top: 1rem;
  }
`;

export default StyledMenuSection;
