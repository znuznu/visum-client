export interface Fonts {
  logo: string;
  main: string;
}

export interface Sizes {
  button: string;
  input: string;
  label: string;
  sidebar: {
    title: string;
    item: string;
  };
  text: string;
  visum: string;
}
